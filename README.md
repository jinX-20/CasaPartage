### How to run this project in local system?
- Open command terminal, navigate to the Frontend folder, run 
`npm run dev`. Running on `localhost:3000`
- Open another command terminal, navigate to the Backend folder, run `npm run dev`. Running on `localhost:5000`.

### What's pending?
- Use JWT token to route after logging in to the expense tracker.
- Create a initial dashboard with desired features including expense tracker.
- My Account page.
- Create a room and join feature.